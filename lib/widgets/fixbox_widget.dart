import 'package:flutter/cupertino.dart';

class FittBoxWidget extends StatelessWidget {
  const FittBoxWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Column(
        children: [],
      ),
    );
  }
}
